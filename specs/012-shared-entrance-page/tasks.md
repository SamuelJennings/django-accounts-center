# Tasks: A shared entrance page owned by the core package

**Branch**: `012-shared-entrance-page` | **Plan**: [plan.md](plan.md) | **Spec**: [spec.md](spec.md)

Test-first throughout (Article I): each implementation task has a test task before it that fails
for the right reason first.

This file is the task list. Tasks do not get issues — the issue graph is Epic (#19) and its three
story sub-issues, and stops there.

US-1 and US-2 are dispatched as one unit of work. They are the two halves of a single move — the
core page does not exist until US-1 lands, and it has no consumer until US-2 does — so splitting
them across worktrees would mean the second rebasing onto the first for no gain.

---

## US-1 — [Developer] An entrance page the core package owns (P1, issue #21)

- **T001** Write failing markup-contract tests in `tests/test_components/test_entrance.py`: a bare
  `{% extends "dac/entrance.html" %}` string renders the full-screen background, one centered card,
  the site logo and the content block's contents; the stylesheet link is present; the messages
  region renders. Covers FR-001, FR-003, FR-009, FR-011.
- **T002** Add `dac/templates/cotton/dac/entrance.html`: `<c-entrance>` with the `size` mapping, the
  site logo above the slot, nothing else. No class authored here (Article XVII). Covers FR-001.
- **T003** Add `dac/templates/dac/entrance.html`: extends `mvp/base.html`, overrides `{% block styles %}`
  with the package stylesheet, renders the messages region, and wraps `{% block content %}` in
  `{% block entrance %}` → `<c-dac.entrance>`. Covers FR-002, FR-003, FR-009, FR-011.
- **T004** Add a guardrail test to `tests/test_architecture.py`: the core entrance templates
  reference no template path under an integration directory, so the page stands up with no
  integration installed. Covers FR-010, SC-004.
- **T005** Reconcile the **Entrance layout** entry in `CONTEXT.md`: it is a core-owned page reached
  by any integration, not something that exists only inside `dac.allauth`. Name the two files.
  Covers FR-013.

## US-2 — [End User] Signed-out pages are unchanged (P1, issue #22)

- **T006** Capture the rendered markup of every anonymous allauth page (login, signup, password
  reset and its done page, sign-in code request and confirm, and any other the installed version
  serves) before touching the layout. Assert post-change output is identical to the capture. The
  capture is generated by the task, not committed as a standing baseline. Covers FR-008, SC-002.
- **T007** Rewire `dac/allauth/templates/allauth/layouts/entrance.html` to
  `{% extends "dac/entrance.html" %}`, keeping only the allauth block mapping (`head_title`,
  `extra_head`, `extra_body`). Remove its card, background, logo and stylesheet markup. Covers
  FR-007.
- **T008** Add a test asserting the allauth entrance layout contains no card, background or logo
  markup of its own. Covers FR-007, SC-005.
- **T009** Confirm `tests/test_architecture.py` is green unchanged: no new page fork, and `base`,
  `entrance` and `manage` still resolve as layout overrides. Covers FR-012.

## US-3 — [Developer] A page asks for the card width it needs (P2, issue #23)

- **T010** Write failing tests: a layout overriding `{% block entrance %}` with `size="full"`
  renders a wider card than the default; a layout that overrides nothing renders today's width; the
  two differ. Covers FR-004, FR-005, SC-003.
- **T011** Implement `size` on `c-dac.entrance` — default today's width, accepted values `sm` and
  `full`, mapped onto mvp's `small`. An unrecognised value falls back to the default rather than
  emitting broken markup. Covers FR-004, FR-005, FR-006.
- **T012** Document the entrance page as an extension point: how a page reaches it, what it fills
  in, how a layout declares a width, and that the range is two values until django-mvp#126 lands
  (tracked at #20). Covers FR-014, SC-006.

## Cross-cutting (after the stories)

- **T013** `npm run build:css` — templates changed, so the shipped stylesheet is rebuilt (Article XV).
- **T014** CHANGELOG entry.
- **T015** Browser pass on login, signup, password reset and sign-in code at both canonical
  viewports (Article XIII, interactive verification — not the screenshots suite).
