name: Tests

on:
  push:
    branches:
      - main
    paths:
      - 'dac/**'
      - 'tests/**'
      - 'pyproject.toml'
      - 'poetry.lock'
      - '.github/workflows/tests.yml'
  pull_request:
    types: [opened, synchronize, reopened]
    paths:
      - 'dac/**'
      - 'tests/**'
      - 'pyproject.toml'
      - 'poetry.lock'
      - '.github/workflows/tests.yml'
  workflow_dispatch:

jobs:
  call-tests:
    uses: django-mvp/.github/workflows/tests.yml@main
    with:
      coverage-package: dac
      install-playwright: true
    secrets: inherit
